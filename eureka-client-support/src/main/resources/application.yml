server:
  port: 8701

eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8700/eureka
  instance:
    hostname: localhost

spring:
  application:
    name: eureka-client-support